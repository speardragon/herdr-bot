{
  "name": "herdr-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "herdr-bot": "./src/main.ts" },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json"
  }
}
